# Инструкция по управлению БД через GitHub Actions

## Автоматические миграции

При каждом push в `main` с изменениями в `supabase/migrations/**`:
- GitHub Actions автоматически проверяет конфликты миграций
- Если найдены старые миграции на удаленной БД, которых нет локально - они автоматически маркируются как `reverted`
- Затем применяются новые миграции

## Полное пересоздание БД (Manual Reset)

### Когда использовать:
- При полном изменении структуры БД
- Когда автоматический repair не помогает
- Для синхронизации production с локальной разработкой

### Как запустить:

1. Перейдите в GitHub: `https://github.com/rshstkv/assisted-orders-nextjs/actions`

2. Выберите workflow "Supabase Migrations"

3. Нажмите "Run workflow" (справа вверху)

4. Установите галочку "Reset database" ⚠️ **ВНИМАНИЕ: Все данные будут удалены!**

5. Нажмите "Run workflow"

### Что происходит при reset:

1. ✅ Подключение к удаленной БД
2. 🔴 Удаление всех таблиц, схем, данных
3. 📦 Создание чистой БД из всех миграций по порядку
4. ✅ База полностью синхронизирована с локальной

## Workflow после reset

После успешного reset все последующие push будут работать в обычном режиме:
- Новые миграции применяются автоматически
- Конфликты разрешаются автоматически
- Данные не теряются (только новые миграции)

## Локальная разработка

### Создание новой миграции:
```bash
supabase migration new migration_name
```

### Применение миграций локально:
```bash
supabase db reset
```

### Коммит и push:
```bash
git add supabase/migrations/
git commit -m "feat: add new migration"
git push
```

GitHub Actions автоматически применит миграцию на production.

## Troubleshooting

### Если workflow падает с ошибкой конфликтов:
1. Сначала попробуйте запустить workflow повторно (может помочь автоматический repair)
2. Если не помогло - используйте manual reset с галочкой "Reset database"

### Если нужно откатить миграцию:
```bash
# Локально
supabase migration repair --status reverted MIGRATION_VERSION

# На production - через manual reset
```

### Проверка статуса миграций:
```bash
# Локально
supabase migration list

# На production - смотрите логи GitHub Actions
```

