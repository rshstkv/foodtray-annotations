name: Disabled Vercel Deploy (use Vercel Git Integration)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Vercel Git Integration handles deploys on push to main."


