# Исправление: Фильтрация bbox

## Проблема
После рефакторинга была удалена функциональность фильтрации bbox, что делало работу менее удобной - все bbox отображались одновременно, даже когда был выбран один.

## Решение

### 1. Возвращена фильтрация bbox

**Добавлено:**
- State `showOnlySelected` (по умолчанию `true`)
- Функция `getFilteredAnnotations()` - фильтрует bbox по выбранному
- Кнопка "👁️ Показать все bbox" / "👁️ Только выбранное" в левой панели

**Логика работы:**
- По умолчанию показывается только выбранный bbox
- При клике на любой bbox в левой панели → включается фильтрация
- При клике на изображение → включается фильтрация
- Hotkey `H` → переключение фильтрации
- Hotkey `Escape` → выключает фильтрацию (показывает все)

### 2. Обновлены динамические hints

**Было:**
```
1-9 выбрать блюдо | 2,2,2 циклично по bbox | D рисовать | Del удалить | Esc сброс
```

**Стало:**
```
1-9 выбрать блюдо | 2,2,2 циклично по bbox | D рисовать | H показать/скрыть все | Del удалить | Esc сброс
```

Когда bbox выбран:
```
2 следующий bbox блюда #2 | Del удалить | H показать все | Esc снять выделение
```

### 3. Автоматическое включение фильтрации

Фильтрация автоматически включается когда:
- Выбран bbox через hotkey (1-9)
- Кликнули на bbox в левой панели
- Кликнули на bbox на изображении
- Кликнули на заголовок блюда в левой панели

### 4. Горячие клавиши

**Новая клавиша:**
- `H` - Показать/скрыть все bbox (toggle)

**Обновленные:**
- `Escape` - Сбросить выделение + показать все bbox
- `1-9` - Выбрать блюдо + включить фильтрацию

## Преимущества

✅ **Удобнее рисовать** - не мешают другие bbox  
✅ **Лучше видно** - фокус на одном элементе  
✅ **Быстрое переключение** - клавиша `H` для показа всех  
✅ **Интуитивно** - автоматически включается при выборе  

## Использование

### Сценарий 1: Работа с конкретным блюдом
1. Нажмите `2` → выбирается первый bbox блюда #2
2. Автоматически показывается только этот bbox
3. Нажмите `2` снова → следующий bbox блюда #2
4. Нажмите `H` → показать все bbox (если нужно)
5. Нажмите `Escape` → сброс + показать все

### Сценарий 2: Рисование нового bbox
1. Нажмите `H` → покажутся все bbox (если они скрыты)
2. Нажмите `D` → режим рисования
3. Нарисуйте bbox
4. Выберите тип в dropdown
5. Новый bbox создан и выбран (показывается только он)

### Сценарий 3: Быстрый просмотр
1. Нажмите `H` → показать все bbox
2. Кликните на нужный bbox
3. Автоматически скроются все остальные

## Файлы изменены
- `src/app/annotations/[id]/page.tsx`

## Изменения в коде

### Добавлено состояние:
```typescript
const [showOnlySelected, setShowOnlySelected] = useState(true)
```

### Добавлена функция фильтрации:
```typescript
const getFilteredAnnotations = (annotations: Annotation[]) => {
  if (!showOnlySelected || !selectedAnnotation) {
    return annotations
  }
  return annotations.filter(a => a.id === selectedAnnotation.id)
}
```

### Добавлена кнопка в левой панели:
```typescript
<Button
  className="w-full mb-4"
  variant={showOnlySelected ? 'outline' : 'default'}
  size="sm"
  onClick={() => setShowOnlySelected(!showOnlySelected)}
>
  {showOnlySelected ? '👁️ Показать все bbox' : '👁️ Только выбранное'}
</Button>
```

### Добавлена горячая клавиша:
```typescript
{
  key: 'h',
  handler: () => {
    setShowOnlySelected(!showOnlySelected)
  }
}
```

## Тестирование

Проверьте:
- ✅ При выборе bbox через `1-9` показывается только он
- ✅ Клик на bbox в левой панели → фильтрация включается
- ✅ Клик на bbox на изображении → фильтрация включается
- ✅ Кнопка "👁️" переключает режим
- ✅ Hotkey `H` переключает режим
- ✅ Hotkey `Escape` → показывает все + сбрасывает выделение
- ✅ При рисовании нового bbox удобно работать с фильтрацией

