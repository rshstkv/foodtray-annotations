# 🚀 План раскатки фичи "Выбор правильного блюда" в Production

## 📋 Что будет происходить автоматически

### При `git push origin main`:

1. ✅ **GitHub Actions автоматически запустится**
   - Файл: `.github/workflows/supabase-migrations.yml`
   - Триггер: изменения в `supabase/migrations/**`

2. ✅ **Миграции применятся в таком порядке:**
   ```
   20251010000000_create_menu_items.sql       → Создает таблицу menu_items (ПУСТАЯ)
   20251010000001_create_correct_dishes.sql   → Создает таблицу correct_dishes (ПУСТАЯ)
   20251010000002_update_clarifications_view.sql → Пересоздает view (DOWNTIME ~100-500ms)
   ```

3. ⚠️ **Фронтенд НЕ деплоится автоматически!** (нужно сделать вручную)

---

## 🎯 ПОШАГОВЫЙ ПЛАН РАСКАТКИ

### ЭТАП 1: Предварительная подготовка (локально)

```bash
# 1. Убедитесь что все изменения закоммичены
git status

# 2. Убедитесь что вы в main ветке
git checkout main

# 3. Смержите фичевую ветку
git merge feature/correct-dish-selection

# 4. Проверьте что все собирается локально
npm run build
```

---

### ЭТАП 2: Бэкап БД (КРИТИЧНО!)

**ОБЯЗАТЕЛЬНО сделайте бэкап ПЕРЕД раскаткой!**

#### Вариант А: Через Supabase Dashboard
1. Откройте https://supabase.com/dashboard/project/YOUR_PROJECT_ID
2. Database → Backups
3. Create backup → Сохраните

#### Вариант Б: Через CLI
```bash
# Подключитесь к проду
supabase link --project-ref YOUR_PROJECT_ID

# Создайте дамп
pg_dump "postgresql://postgres:[PASSWORD]@db.[PROJECT_REF].supabase.co:5432/postgres" \
  > backup_before_correct_dish_$(date +%Y%m%d_%H%M%S).sql
```

---

### ЭТАП 3: Раскатка миграций

```bash
# 1. Пушим в main (миграции применятся автоматически!)
git push origin main

# 2. Следите за GitHub Actions
# https://github.com/YOUR_USERNAME/YOUR_REPO/actions

# 3. Дождитесь зелёного статуса ✅
```

**⏱️ Downtime:** ~100-500ms (при пересоздании view)

**Что проверить в логах:**
```
✓ Link project
✓ Push migrations to remote
  - Applying 20251010000000_create_menu_items.sql... ✓
  - Applying 20251010000001_create_correct_dishes.sql... ✓
  - Applying 20251010000002_update_clarifications_view.sql... ✓
```

---

### ЭТАП 4: Импорт menu_items (ВАЖНО!)

**⚠️ БЕЗ ЭТОГО ШАГА ФИЧА НЕ БУДЕТ РАБОТАТЬ!**

#### Способ 1: Локально с подключением к проду

```bash
# 1. Экспортируйте env переменные для прода
export NEXT_PUBLIC_SUPABASE_URL="https://YOUR_PROJECT.supabase.co"
export NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJhb..."

# 2. Запустите импорт с указанием пути к CSV
npm run import:menu "/path/to/Pingo Doce menu.csv"

# 3. Проверьте результат
# Должно быть: "🎉 Import complete! Inserted 706 menu items"
```

#### Способ 2: На сервере (если фронтенд на сервере)

```bash
# 1. Загрузите CSV на сервер
scp "Pingo Doce menu.csv" user@server:/tmp/menu.csv

# 2. SSH на сервер
ssh user@server

# 3. Перейдите в директорию проекта
cd /path/to/assisted-orders-nextjs

# 4. Запустите импорт
npm run import:menu /tmp/menu.csv
```

---

### ЭТАП 5: Проверка после миграций

```sql
-- Подключитесь к БД прода
psql "postgresql://postgres:[PASSWORD]@db.[PROJECT_REF].supabase.co:5432/postgres"

-- 1. Проверьте что таблицы созданы
\dt menu_items
\dt correct_dishes

-- 2. Проверьте что menu_items заполнена
SELECT COUNT(*) FROM menu_items;
-- Ожидаемый результат: 706 (или сколько там блюд)

-- 3. Проверьте что view работает
SELECT COUNT(*) FROM clarifications_with_bucket;
-- Ожидаемый результат: текущее количество кларификаций

-- 4. Проверьте новые поля в view
SELECT 
  COUNT(*) as total,
  COUNT(correct_dish_ean) as with_correct_dish,
  COUNT(actual_correct_ean) as with_actual_correct
FROM clarifications_with_bucket;

-- 5. Проверьте что старые данные на месте (КРИТИЧНО!)
SELECT COUNT(*) FROM clarifications;
SELECT COUNT(*) FROM orders;
SELECT COUNT(*) FROM clarification_states;
```

---

### ЭТАП 6: Деплой фронтенда

**⚠️ Зависит от вашего хостинга:**

#### Если используете Vercel:
```bash
# Push в main автоматически задеплоит через Vercel GitHub integration
git push origin main
# Или вручную:
vercel --prod
```

#### Если используете Netlify:
```bash
netlify deploy --prod
```

#### Если собственный сервер:
```bash
# На сервере:
git pull origin main
npm install
npm run build
pm2 restart assisted-orders  # или systemctl restart
```

---

### ЭТАП 7: Проверка в проде (ОБЯЗАТЕЛЬНО!)

1. **Откройте приложение в проде**
   ```
   https://your-prod-url.com
   ```

2. **Проверьте основной функционал:**
   - ✅ Страница загружается без ошибок
   - ✅ Кларификации отображаются
   - ✅ Фильтры работают
   - ✅ **Новый фильтр "corrected" появился в dropdown**

3. **Проверьте новую фичу:**
   - ✅ Найдите карточку с state="no" (красная)
   - ✅ Кликните на доступный вариант → карточка становится синей ✅
   - ✅ Нажмите "🔍 Найти в меню" → должно открыться окно с ~706 блюдами ✅
   - ✅ Выберите блюдо из меню → карточка становится синей ✅
   - ✅ Нажмите "Очистить" → карточка становится красной ✅
   - ✅ Обновите страницу → выбор сохранился ✅

4. **Проверьте что ничего не сломалось:**
   - ✅ Кнопки YES/NO работают как раньше
   - ✅ Clear работает
   - ✅ Фильтры работают
   - ✅ Infinite scroll работает

5. **Проверьте в консоли браузера:**
   ```javascript
   // Не должно быть ошибок 404 или 500
   // Проверьте API запросы в Network tab
   ```

---

## 🚨 Что делать если что-то пошло не так

### Проблема 1: Миграции не применились

```bash
# 1. Проверьте логи GitHub Actions
# 2. Если ошибка - откатите коммит:
git revert HEAD
git push origin main

# 3. Восстановите из бэкапа если нужно
```

### Проблема 2: View не работает

```sql
-- Пересоздайте view вручную (скопируйте из миграции)
DROP VIEW IF EXISTS clarifications_with_bucket;
CREATE VIEW ...
```

### Проблема 3: Фронтенд не видит новые поля

```bash
# Проверьте что фронтенд задеплоен:
# 1. Проверьте версию в проде
# 2. Форсированно передеплойте
vercel --prod --force
```

### Проблема 4: menu_items пустая

```bash
# Просто запустите импорт заново
npm run import:menu "/path/to/menu.csv"
```

---

## 📊 Rollback план

### Если нужно откатить ПОЛНОСТЬЮ:

```bash
# 1. Откатите фронтенд к предыдущей версии
git revert HEAD~10..HEAD  # откатить последние 10 коммитов
git push origin main

# 2. Откатите миграции
supabase db reset --db-url "postgresql://..."

# 3. Восстановите из бэкапа
psql "postgresql://..." < backup_before_correct_dish_*.sql
```

### Если нужно откатить только фичу (оставить миграции):

```sql
-- Таблицы можно оставить, они не мешают
-- Просто откатите фронтенд
```

---

## ✅ Чеклист перед раскаткой

- [ ] Сделан бэкап БД
- [ ] Все тесты прошли локально
- [ ] Проверили что миграции применяются локально (`npm run db:migrate`)
- [ ] Подготовлен CSV файл для импорта
- [ ] Убедились что в нерабочее время (минимальный риск)
- [ ] Команда в курсе (если есть)
- [ ] Есть план отката
- [ ] Проверили что env переменные настроены для прода

---

## 📞 Контакты на случай проблем

- GitHub Actions: https://github.com/YOUR_USERNAME/YOUR_REPO/actions
- Supabase Dashboard: https://supabase.com/dashboard
- Логи фронтенда: (зависит от хостинга)

---

## ⏰ Рекомендуемое время раскатки

- **Лучшее время:** Выходные или нерабочее время (минимум пользователей)
- **Плохое время:** Рабочие часы (пятница вечер)
- **Требуемое время:** ~10-15 минут на всю раскатку

---

## 📝 Дополнительные заметки

1. **Совместимость:** Фича полностью обратно-совместима
2. **Производительность:** View пересоздается быстро, индексы на месте
3. **Данные:** Существующие данные НЕ затрагиваются
4. **Новые данные:** correct_dishes и menu_items - новые таблицы

---

**Успешной раскатки! 🚀**

