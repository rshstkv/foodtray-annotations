# Фича выбора правильного блюда

## Что реализовано

Добавлена возможность выбрать правильное блюдо, когда пользователь нажимает **NO**:

1. **База данных:**
   - Таблица `menu_items` - справочник всех блюд из меню
   - Таблица `correct_dishes` - хранит выбранные вручную правильные блюда
   - Обновлён view `clarifications_with_bucket` с полями:
     - `correct_dish_ean`, `correct_dish_name`, `correct_dish_source`
     - `actual_correct_ean` (computed) - ground truth для обучения модели

2. **Backend API:**
   - `GET /api/menu-items?search={query}` - поиск блюд по названию или EAN
   - `POST /api/correct-dishes` - сохранение выбранного правильного блюда
   - `DELETE /api/correct-dishes` - удаление выбора

3. **Frontend:**
   - Компонент `MenuSearchDialog` - попап с поиском по всему меню
   - Обновлённый UI `ClarificationCard`:
     - При `state='no'` появляется секция выбора правильного блюда
     - Можно кликнуть на любой вариант из available_products (белые карточки)
     - Кнопка "Найти в меню" открывает полный справочник с поиском
     - Выбранное блюдо отмечается зелёным с галочкой ✓

## Логика работы

### Правильное блюдо (ground truth):
1. **state = 'yes'** → правильное блюдо = `ean_matched[0]` (распознанное)
2. **state = 'no'** + есть `correct_dishes` → правильное блюдо = `selected_ean` (вручную выбранное)
3. **state = 'no'** + нет `correct_dishes` → правильное блюдо неизвестно

## Установка и запуск

### 1. Запустите Docker Desktop

### 2. Примените миграции:
```bash
npx supabase db reset
```

### 3. Импортируйте меню из CSV:
```bash
npx tsx scripts/import-menu.ts
```

### 4. Запустите dev сервер:
```bash
npm run dev
```

## Тестирование

### Flow 1: Выбор из доступных вариантов
1. Откройте приложение
2. Найдите кларификацию и нажмите **NO**
3. Появится секция "Выберите правильное блюдо"
4. Кликните на любую **белую карточку** (available_products)
5. Карточка станет зелёной с галочкой ✓
6. Выбор автоматически сохраняется в БД

### Flow 2: Поиск в меню
1. Нажмите **NO** на кларификации
2. Нажмите кнопку **🔍 Найти в меню**
3. Откроется попап с поиском
4. Введите название блюда (например, "bacalhau") или EAN код
5. Результаты появятся в реальном времени (debounce 300ms)
6. Нажмите **Выбрать** на нужном блюде
7. Попап закроется, выбранное блюдо отобразится зелёным

### Flow 3: Изменение выбора
1. Если блюдо уже выбрано, отображается панель с информацией
2. Нажмите **Изменить**
3. Можно выбрать новое блюдо любым способом
4. Предыдущий выбор обновляется (upsert в БД)

### Flow 4: Состояние сохраняется
1. Выберите правильное блюдо
2. Обновите страницу (F5)
3. Выбор остаётся (данные из БД)

## Файлы проекта

**Миграции:**
- `supabase/migrations/20251010000000_create_menu_items.sql`
- `supabase/migrations/20251010000001_create_correct_dishes.sql`
- `supabase/migrations/20251010000002_update_clarifications_view.sql`

**Скрипты:**
- `scripts/import-menu.ts` - импорт CSV в menu_items

**API:**
- `src/app/api/menu-items/route.ts` - поиск блюд
- `src/app/api/correct-dishes/route.ts` - сохранение выбора
- `src/app/api/clarifications/route.ts` - обновлён для возврата correct_dish полей

**Компоненты:**
- `src/components/MenuSearchDialog.tsx` - диалог поиска меню
- `src/app/page.tsx` - обновлён ClarificationCard

**Типы:**
- `src/hooks/useInfiniteClarifications.ts` - добавлены типы MenuItem, CorrectDish

## Edge cases

✅ Кларификация без available_products → показывается только кнопка "Найти в меню"
✅ Изменение state с 'no' на 'yes' → выбор остаётся в БД, но UI скрывается
✅ Множественные выборы → только последний (upsert по clarification_id)
✅ ESC закрывает диалог поиска
✅ Клик вне диалога закрывает его
✅ Поиск работает по частичному совпадению названия (case-insensitive)
✅ Поиск по EAN работает как точное совпадение (regex /^\d+$/)
✅ Дебаунс 300ms для живого поиска

## Ground Truth для ML

Поле `actual_correct_ean` в view содержит финальное правильное блюдо:
- Используйте для экспорта датасета
- Учитывает логику: YES = распознанное, NO + выбор = вручную исправленное

## Следующие шаги

- [ ] Добавить изображения блюд в `menu_items.reference_image_url`
- [ ] Добавить экспорт ground truth для обучения модели
- [ ] Добавить статистику по правильным блюдам
- [ ] Добавить bulk операции для множественного выбора

