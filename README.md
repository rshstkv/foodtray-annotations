# RRS Product Clarifications - Enhanced Version

Улучшенная версия приложения для обработки кларификаций заказов ресторанов с автоматической подгрузкой данных и расширенной фильтрацией.

## ✨ Новые возможности

### 🔄 Автоматическая подгрузка (Infinite Scroll)
- **Пагинация**: Загрузка данных порциями по 25 записей
- **Автоматическая подгрузка**: При скролле до конца автоматически загружаются следующие записи
- **Индикаторы загрузки**: Skeleton loader и спиннер для плавного UX
- **Оптимизация**: Прерывание предыдущих запросов при изменении фильтров

### 🔍 Расширенная фильтрация
- **Фильтры заказов**:
  - Столовая (множественный выбор)
  - Диапазон дат заказа
  - Наличие событий ассистента (да/нет)
  - Тип изображения (main/qualifying)

- **Фильтры кларификаций**:
  - Тип кларификации (множественный выбор)
  - Поиск по названию продукта (с debounce 300ms)
  - Суперкласс и гиперкласс
  - Диапазон количества EAN совпадений
  - Найдено ли изображение (да/нет)

- **Фильтры состояний**:
  - Состояние (yes/no/не задано)
  - Диапазоны дат создания и обновления

### 🎨 UI/UX улучшения
- **Адаптивный дизайн**: Оптимизирован для мобильных устройств
- **Сворачиваемая панель фильтров**: На мобильных устройствах
- **Счетчик активных фильтров**: Показывает количество примененных фильтров
- **Индикаторы**: Показ общего количества найденных записей
- **URL синхронизация**: Состояние фильтров сохраняется в URL

### ⚡ Производительность и оптимизация
- **Debounced поиск**: Снижение нагрузки на сервер
- **Отмена запросов**: Автоматическая отмена предыдущих запросов
- **Мемоизация**: Оптимизация ре-рендеров React компонентов  
- **Кеширование**: Результаты фильтрации
- **Error Boundary**: Обработка ошибок на уровне компонентов

## 🏗️ Архитектура

### Хуки
- `useInfiniteClarifications` - Управление бесконечной прокруткой и данными
- `useFilters` - Управление фильтрами и синхронизация с URL
- `useDebouncedValue` - Debounce для оптимизации поиска
- `useLocalStorageState` - Синхронизация с localStorage

### API Endpoints
- `GET /api/clarifications` - Получение кларификаций с пагинацией и фильтрами
- `GET /api/filter-options` - Получение уникальных значений для фильтров
- `GET/POST/DELETE /api/states` - Управление состояниями кларификаций

### Компоненты
- `FilterPanel` - Главная панель фильтров
- `MultiSelectFilter` - Множественный выбор с поиском
- `DateRangeFilter` - Фильтр диапазона дат
- `NumberRangeFilter` - Фильтр числового диапазона
- `InfiniteScroll` - Компонент для бесконечной прокрутки
- `LoadingIndicator` - Индикаторы загрузки
- `ErrorBoundary` - Обработка ошибок

## 📊 Суpabase Query Оптимизация

### Индексы (рекомендуемые)
```sql
-- Для быстрой фильтрации по датам
CREATE INDEX idx_orders_start_dtts ON orders(start_dtts);

-- Для поиска по названию продукта
CREATE INDEX idx_clarifications_product_name ON clarifications USING gin(product_name gin_trgm_ops);

-- Композитные индексы для частых фильтров
CREATE INDEX idx_clarifications_type_superclass ON clarifications(clarification_type, superclass);
CREATE INDEX idx_orders_canteen_sign ON orders(device_canteen_name, sign);

-- Для LEFT JOIN с состояниями
CREATE INDEX idx_clarification_states_clarification_id ON clarification_states(clarification_id);
```

### Оптимизация запросов
- Используется `select` только необходимых полей
- Применение `inner join` для фильтрации
- `LEFT JOIN` для опциональных данных состояний
- `count: 'exact'` только при необходимости
- Пагинация через `.range()`

## 🚀 Запуск приложения

### Установка зависимостей
```bash
npm install
```

### Настройка окружения
Убедитесь что в `.env.local` настроены:
```
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### Запуск в разработке
```bash
npm run dev
```

### Запуск в продакшене
```bash
npm run build
npm start
```

## 🧪 Тестирование

### Производительность
- Тестирование загрузки больших объемов данных (10000+ записей)
- Проверка работы фильтров при медленном интернете
- Тестирование на мобильных устройствах

### Функциональность
- Все комбинации фильтров работают корректно
- URL синхронизация работает при перезагрузке
- Состояния сохраняются и загружаются правильно
- Экспорт работает с отфильтрованными данными

## 📱 Поддерживаемые браузеры
- Chrome 90+
- Firefox 85+
- Safari 14+
- Edge 90+

## 🔧 Конфигурация

### Константы в `/src/lib/constants.ts`
- Размер страницы пагинации: `PAGINATION.DEFAULT_PAGE_SIZE = 25`
- Задержка debounce поиска: `DEBOUNCE_DELAYS.SEARCH = 300`
- Breakpoint для мобильных: `UI_CONFIG.MOBILE_BREAKPOINT = 768`

### Настройка фильтров
Значения по умолчанию и логика в `useFilters.ts`

## 🐛 Известные ограничения
- Максимум 100 элементов на страницу для предотвращения блокировки UI
- Debounce поиска может казаться медленным на очень быстром наборе
- На старых мобильных браузерах могут быть проблемы с Intersection Observer

## 🔄 Миграция данных
При обновлении с предыдущей версии:
1. Новые поля в API response автоматически обрабатываются
2. Старые URL параметры совместимы
3. LocalStorage ключи не изменились

## 🤝 Вклад в разработку
1. Убедитесь что код проходит TypeScript проверки
2. Добавляйте JSDoc комментарии для новых функций
3. Тестируйте на мобильных устройствах
4. Используйте существующие UI компоненты из shadcn/ui