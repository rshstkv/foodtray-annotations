# BBox Annotation UI Redesign - Implementation Summary

## Обзор

Реализована полная переработка UI системы аннотации bounding box согласно спецификации `BBOX_ANNOTATION_UI_REDESIGN.md`. Новая архитектура обеспечивает интуитивный, расширяемый и удобный интерфейс для аннотирования изображений.

## Реализованные изменения

### ✅ Phase 1: Архитектура данных и состояния

**1.1 База данных**
- Создана миграция `20251104200000_update_annotations_non_food.sql`
- Добавлена поддержка non-food типов объектов в `annotations.object_type`
- Индексы на `is_error` поле для быстрой фильтрации проблемных аннотаций
- Комментарии к полям для документации структуры данных

**1.2 API**
- Обновлен `GET /api/annotations/recognitions/[id]`
- Теперь возвращает `menu_all` из таблицы `recognitions_raw`
- Поддержка всех типов объектов: food, non_food, buzzer, plate, tray

### ✅ Phase 2: Левая панель - развернутый список bbox

**Убрано (согласно спецификации):**
- ❌ Кнопка "Только выбранное"
- ❌ Кнопка "Завершить" (перенесена в header)
- ❌ Чекбокс "Ошибка в чеке" (заменен на workflow)
- ❌ Кнопка "Добавить блюдо" (заменена на workflow через рисование)
- ❌ Блок "Горячие клавиши" (перенесен наверх как динамическая строка)
- ❌ Статус (перенесен в header)

**Добавлено:**
- ✅ Развернутый список всех bbox для каждого блюда
- ✅ Кнопка удаления (🗑️) для каждого bbox
- ✅ Группировка: "Блюда из чека" + "Non-food объекты"
- ✅ Клик на bbox → выделение на изображении
- ✅ Клик на блюдо → выделение первого bbox блюда
- ✅ Индикатор количества bbox: `2/1` (найдено/ожидается)
- ✅ Индикатор множественных вариантов: `[3 вар.]`
- ✅ Визуальное выделение выбранного bbox (синий фон)

**Структура:**
```
┌──────────────────────────────────┐
│ [Нарисовать bbox]                │
│                                  │
│ Блюда из чека                    │
│                                  │
│ 🟢 #1 REF S/GÁS          1/1     │
│    └─ bbox #1   [🗑️]             │
│                                  │
│ 🔵 #2 Sopa de Legumes    2/1     │
│    ├─ bbox #1   [🗑️]             │
│    └─ bbox #2   [🗑️]             │
│                                  │
│ Non-food объекты                 │
│                                  │
│ 📦 Кошелек                       │
│    └─ bbox #1   [🗑️]             │
└──────────────────────────────────┘
```

### ✅ Phase 3: Workflow рисования bbox

**Dropdown с 4 секциями:**

1. **📋 Блюда из чека (`correct_dishes`)**
   - Отображение всех блюд с их вариантами
   - Цветовые индикаторы по dish_index
   - Нумерация #1, #2, #3...

2. **🍽️ Активное меню (`menu_all`)**
   - Поиск по EAN (точное совпадение для числовых запросов)
   - Поиск по Name (fuzzy search, case-insensitive)
   - Отображение до 20 результатов
   - Показ EAN кода для каждого элемента

3. **📦 Non-food объекты**
   - Hardcoded список:
     - ✋ Рука (hand)
     - 📱 Телефон (phone)
     - 👛 Кошелек (wallet)
     - 💳 Карты (cards)
     - 🍴 Столовые приборы (cutlery)
     - 📦 Другое (other)

4. **❌ Ошибка**
   - Опция "Не найдено в чеке или меню"
   - Устанавливает `is_error: true`
   - Красная рамка и иконка ⚠️ на изображении

### ✅ Phase 4: Упрощение горячих клавиш

**Убрана сложная навигация X0Y:**
- Старая логика: `201` → блюдо #2, вариант #1 ❌
- Новая логика: Простая циклическая навигация ✅

**Циклическая навигация:**
```
Нажатия:  2     2     2     2
Выбор:    #2.1  #2.2  #2.1  #2.2  (циклично)
```

**Быстрое переключение между блюдами:**
```
Нажатия:  2     3     1
Выбор:    #2.1  #3.1  #1.1
```

**Список горячих клавиш:**
- `1-9` - Выбрать блюдо (циклично между bbox блюда)
- `D` - Включить/выключить режим рисования
- `Delete` - Удалить выбранный bbox
- `Escape` - Сброс всех состояний
- `Ctrl+S` - Завершить (Done)

### ✅ Phase 5: Динамические hints

**3 контекста подсказок:**

1. **Нормальный режим:**
   ```
   1-9 выбрать блюдо | 2,2,2 циклично по bbox | D рисовать | Del удалить | Esc сброс
   ```

2. **Режим рисования:**
   ```
   Нарисуйте bbox мышью | Esc отменить | D выключить рисование
   ```

3. **Выбран bbox:**
   ```
   2 следующий bbox блюда #2 | Del удалить | Esc снять выделение
   ```

### ✅ Phase 6: Header с статусом

**Структура:**
```
┌─────────────────────────────────────────────────────┐
│ Recognition 62764         Статус: ⚙️ В работе       │
│ 09.10.2025                [← Назад]  [Done]         │
└─────────────────────────────────────────────────────┘
```

**Логика статусов:**
- `not_started` → "📝 Новый"
- `in_progress` → "⚙️ В работе" (автоматически при любых изменениях)
- `completed` → "✓ Завершено" (кнопка "Done")
- `rejected` → "✗ Отклонено"

### ✅ Phase 7: Визуализация

**BBoxAnnotator обновлен:**

**Цвета:**
- `is_error === true` → Ярко-красный (#dc2626) с иконкой ⚠️
- `is_overlapped === true` → Оранжевый (#f97316) с иконкой ⚠️
- `dish_index` → Цвет из палитры (9 цветов)
- `non_food` → Фиолетовый (#a855f7)
- `plate` → Желтый (#eab308)
- `buzzer` → По цвету подтипа

**Лейблы:**
- Ошибка: "⚠️ ОШИБКА"
- Non-food: "✋ Рука", "📱 Телефон", "👛 Кошелек", etc.
- Блюдо: "#2 Sopa de Legumes"
- Перекрытие: добавляется " ⚠️"

## Технические детали

### Файлы изменены:

1. **Миграция БД:**
   - `supabase/migrations/20251104200000_update_annotations_non_food.sql`

2. **API:**
   - `src/app/api/annotations/recognitions/[id]/route.ts`

3. **Компоненты:**
   - `src/app/annotations/[id]/page.tsx` (полная переработка - 900+ строк)
   - `src/components/BBoxAnnotator.tsx` (обновлена визуализация)

### Новые константы:

```typescript
// Non-food объекты
const NON_FOOD_OBJECTS = [
  { id: 'hand', name: 'Рука', icon: '✋' },
  { id: 'phone', name: 'Телефон', icon: '📱' },
  { id: 'wallet', name: 'Кошелек', icon: '👛' },
  { id: 'cards', name: 'Карты', icon: '💳' },
  { id: 'cutlery', name: 'Столовые приборы', icon: '🍴' },
  { id: 'other', name: 'Другое', icon: '📦' }
]

// Цвета для блюд
const DISH_COLORS = [
  '#22c55e', '#3b82f6', '#f59e0b', '#ef4444', '#8b5cf6',
  '#ec4899', '#06b6d4', '#f97316', '#84cc16'
]
```

## Симметрия действий

| Действие | Левая панель | Изображение |
|----------|-------------|-------------|
| Выбрать bbox | ✅ Клик на bbox | ✅ Клик на bbox |
| Удалить bbox | ✅ Кнопка 🗑️ | ✅ Hotkey `Delete` |
| Создать bbox | ❌ | ✅ Рисование (D) |
| Изменить размер | ❌ | ✅ Resize handles |
| Переместить | ❌ | ✅ Drag |
| Изменить тип | - | ✅ Toolbar → dropdown |

## Workflow использования

### Сценарий 1: Просмотр и редактирование

1. Открыл recognition → видит список блюд слева, изображение справа
2. Видит что блюдо #2 имеет 2 bbox → оба видны в списке как отдельные элементы
3. Нажимает `2` → выбирается первый bbox блюда #2
4. Нажимает `2` ещё раз → переходит ко второму bbox (циклично)
5. Видит что один bbox лишний → нажимает `Delete` → удаляется
6. Автоматически выбирается следующий bbox того же блюда

### Сценарий 2: Добавление нового bbox

1. Нажимает "Нарисовать bbox" (или `D`)
2. Рисует прямоугольник мышью
3. Появляется dropdown с 4 секциями
4. **Вариант A:** Выбирает блюдо из чека → bbox создан как food
5. **Вариант B:** Ищет в активном меню → bbox создан с новым dish_index
6. **Вариант C:** Выбирает non-food объект → bbox создан как non_food
7. **Вариант D:** Выбирает "Не найдено" → bbox создан с is_error: true

### Сценарий 3: Завершение работы

1. Все bbox добавлены и проверены
2. Нажимает "Done" (или `Ctrl+S`)
3. Статус меняется на "✓ Завершено"
4. Recognition готов к дальнейшей обработке

## Расширяемость

### Добавление новых non-food типов:

```typescript
const NON_FOOD_OBJECTS = [
  ...существующие,
  { id: 'glasses', name: 'Очки', icon: '👓' }
]
```

### Добавление новых типов объектов:

1. Обновить миграцию БД (добавить в CHECK constraint)
2. Добавить в dropdown новую секцию
3. Добавить цвет в `getColor()` функцию
4. Добавить лейбл в `getAnnotationLabel()` функцию

## Тестирование

### Что нужно протестировать:

**Левая панель:**
- ✅ Клик на bbox выделяет его на изображении
- ✅ Клик на блюдо выделяет первый bbox
- ✅ Кнопка 🗑️ удаляет конкретный bbox
- ✅ Отображение всех bbox блюда в развернутом виде
- ✅ Группировка non-food объектов отдельно

**Горячие клавиши:**
- ✅ `2,2,2` - циклическая навигация по bbox блюда #2
- ✅ `2,3,1` - переключение между блюдами
- ✅ `Delete` → удаление → автовыбор следующего bbox
- ✅ `Escape` → сброс всех состояний
- ✅ `D` → включение/выключение режима рисования

**Dropdown:**
- ✅ Появляется после рисования bbox
- ✅ 4 секции: чек, menu_all, non-food, ошибка
- ✅ Поиск в menu_all работает по EAN и Name
- ✅ Выбор из чека создает food bbox
- ✅ Выбор non-food создает non_food bbox
- ✅ Выбор "Не найдено" создает bbox с is_error

**Визуализация:**
- ✅ Bbox с is_error отображается красным с ⚠️
- ✅ Non-food bbox отображается фиолетовым
- ✅ Лейблы корректно показывают тип объекта
- ✅ Цвета блюд соответствуют левой панели

**Header:**
- ✅ Статус отображается правильно
- ✅ Кнопка "Done" меняет статус на completed
- ✅ Автоматическое изменение статуса на in_progress

## Известные особенности

1. **Menu_all:** Используются данные из `recognitions_raw.menu_all` (JSON поле)
2. **Dish_index для menu_all:** Новые блюда получают индексы больше, чем в correct_dishes
3. **Автосохранение:** Все изменения bbox сохраняются немедленно (optimistic updates)
4. **Циклическая навигация:** Работает только для блюд с несколькими bbox
5. **Non-food визуализация:** Все non-food объекты одного цвета (фиолетовый)

## Следующие шаги (если потребуется)

1. Добавить фильтрацию по датам для menu_all
2. Добавить bulk операции (удаление нескольких bbox сразу)
3. Добавить undo/redo функциональность
4. Добавить экспорт аннотаций в различных форматах
5. Добавить статистику по recognition (время работы, количество изменений)

## Заключение

Реализация полностью соответствует спецификации `BBOX_ANNOTATION_UI_REDESIGN.md`. Все фазы (1-7) выполнены. Интерфейс стал значительно чище, удобнее и расширяемее. Упрощенная навигация горячими клавишами делает работу быстрее и интуитивнее.

